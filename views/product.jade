extends base/base

block prepend title
	- title = product.name + ' para ' + event.name
	- description = 'Mirá nuestros ' + product.name + ' para ' + event.name

block prepend metas
	- keywords = [event.name, product.name]

block content
	#product.container
		.row
			.col-md-2
				include includes/sidebar
			.col-md-10
				#projects.row
					each project in product.items()
						.col-sm-4
							a(href=project.url)
								img(src=project.thumb())
								p= project.name